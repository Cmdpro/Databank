package com.cmdpro.databank.registry;

public class BlockTemperatureRegistry {
    // TODO data-driven block temperatures (which will be taken into account by blocks checking their temperature later)
}
